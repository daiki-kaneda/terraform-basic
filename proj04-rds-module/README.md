1. RDSリソースについて理解し、必要なリソースについて理解する(完了)
2. 標準的な構成のモジュールを作成する（完了）
3. variableのバリデーションを作る（完了）
4. ネットワークのバリデーションを作成する(完了)
   1. variablesからサブネットIDやセキュリティグループを受け取る(完了)
   2. サブネットは
      1. デフォルトVPCのものではないことをチェックする(完了)
      2. privateである様にする(完了)
         1. Access=Privateタグがついているかをチェックする　(完了)
   3. セキュリティグループについて(完了)
      1. IPアドレスに対するセキュリティグループがない様にする (完了)
5. 必要なリソースを作成して、バリデーションが機能しているかをチェックする（完了）
6. RDSインスタンスをモジュール内で作成する(完了)